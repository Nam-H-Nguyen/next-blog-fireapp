import Loader from '../components/Loader';

export default function Home() {
  return (
    <div>
      {/* <Loader show={true} />
      <Link prefetch={false} href={{
        pathname: '/[username]',
        query: {
          username: 'jeffd23',
        }
      }}>
        <a>Jeff`s profile</a>
      </Link> */}
    </div>
  )
}
